
&НаСервере
Процедура Команда1НаСервере()
	Если Объект.Реализация.Проведен Тогда
		Реализация = Объект.Реализация.ПолучитьОбъект();
		Реализация.Завершен = Истина;
		Реализация.Записать(РежимЗаписиДокумента.Запись);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Команда1(Команда)
	Команда1НаСервере();
КонецПроцедуры
   